# /etc/systemd/system.conf.d/watchdog.conf
# Uses the BCM2711 hardware watchdog: systemd pets it while healthy; if the
# board hard-hangs it stops petting and the watchdog reboots it.
[Manager]
RuntimeWatchdogSec=15
RebootWatchdogSec=2min
